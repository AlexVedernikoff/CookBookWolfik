import classes from './SortRecipes.module.scss';

const SortRecipes = () => {
    return (
        <div className={classes['sorts']} >
            SORTING
        </div>
    );
};

export default SortRecipes;